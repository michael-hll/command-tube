Email: tube-config.yaml
Servers: tube-config.yaml
Tube:
    #- connect: vmlmh6sh03.qad.com --continue
    #- SFTP_GET: -r /dr01/qadapps/systest/servers/tomcat-webui/webapps/qad-central/WEB-INF/config/*.* -l tmp
    #- SFTP_GET: -r /dr01/qadapps/systest/servers/tomcat-webui/webapps/qad-central/WEB-INF/config/*.xml2 -l tmp
    #- SFTP_PUT: -l tmp/*.* -r /dr01/qadapps/systest/dump
    #- CHECK_CHAR_EXISTS: '-f paramiko.log2 -c kex follows: False -r c_exists --continue'
    #- COMMAND: echo run command --if {c_exists}==False
    #- COMMAND: ls --if 2={var2}
    #- PATH: '{TUBE_HOME}/tmp'
    #- REPLACE_CHAR: -f tmp.txt -o asdf -n abcd -c 100
    #- pause: {duration}
    # - SET_VARIABLE: -n code -v 2000 -r
    # - SET_VARIABLE: -n code -v 2000 -rf
    # - SET_VARIABLE: -n code -v 3000
    # - SET_VARIABLE: -n code -v 4000 -f
    # - IMPORT_TUBE: test2.yaml    
    # - PRINT_VARS: '*'
    - CONNECT: linux01
    #- LINUX_COMMAND: yab status --log-detail
    #- COMMAND: echo hello michael
    - LINUX_COMMAND: ls

        

    

